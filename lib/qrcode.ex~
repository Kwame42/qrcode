defmodule MQrcode do
  @moduledoc """
  Documentation for `Qrcode`.
  """

  @doc """
  Hello world.

  ## Examples

      iex> Qrcode.hello()
      :world

  """
  def hello(appellation, annee) do
    "https://aubigny/#{appellation}/#{annee}"
    |> QRCode.create(:high)
    |> QRCode.render(:png)
    |> QRCode.save("QRCODE_#{appellation}_#{annee}.png")

  end
end
